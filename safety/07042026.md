# Safety — 07 Apr 2026

## What I did
- Ported full Asimov velocity policy pipeline from JS → Python (commit `2228b5a`)
  - `vnrap/dc_motor.py`: DC motor model (torque-speed curve + friction)
  - `vnrap/asimov_obs.py`: 78-dim observation builder with CAN-slot reordering
  - `assets/policies/asimov/`: ONNX checkpoint + config for velocity policy
  - `AsimovVelocityPolicy` in `policy.py`: obs → ONNX → LPF → PD → DC motor → ctrl
- Added **live benchmark viewer**: simulation plays each scenario in real-time with cinematic overlays, scorecards, and auto-leaderboard
- Added **policy calibration flow**: iframe of `humanoid-policy-viewer` lets user verify policy before running benchmark (Accept/Reject via HTTP endpoints)
- Shipped `humanoid-policy-viewer` Vue app — Asimov velocity ONNX running in MuJoCo WASM (commit `e088704`, merged as PR #3)
- Fixed double floor plane destabilising the robot
- Fixed benchmark counter off-by-one (was showing 15/14)
- Updated `asimov_full.xml` to match the model the policy was trained on

## Findings / results
- Real ONNX locomotion policy runs live in the benchmark loop (decimation every 10 sim steps)
- Policy calibration iframe confirms the robot is balancing correctly before stress scenarios begin

## Blockers
- None

## Next steps
- Expand scenario coverage (trips, power loss, body slams)
- Tune injury metrics (HIC₁₅, Nij) against live policy results
- Compare active policy vs. passive ragdoll baselines
