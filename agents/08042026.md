# Agents — 08 Apr 2026

## What I did
- Read `../agent-bench/README.md` to confirm current project framing (Skill-as-Bench, maze-bench as first benchmark).
- Pulled latest upstream changes in `../agent-bench` and reviewed newest merged PR on `origin/main`:
  - `1ccdbb9` — **Merge pull request #1** from `feat/maze-scoring`
  - PR summary: add maze run scoring against optimal path.
- Reviewed changed files from PR #1:
  - `benchmarks/maze-bench/skills/maze-game/score_maze_run.py` (new)
  - `benchmarks/maze-bench/skills/maze-game/SKILL.md` (updated with scoring command)
  - `benchmarks/maze-bench/README.md` (updated scoring section)

## Findings / results
- Maze benchmark now includes a built-in scorer that computes shortest command-count path and reports:
  - success/failure
  - steps
  - optimal steps
  - wasted steps
  - efficiency (`optimal/actual`)
  - qualitative rating (Optimal / Good / Completed / Failed)
- Benchmark usability improved: agent tutorial now explicitly includes `score_maze_run.py` in controls and scoring workflow.

## Structure we are building
- `skill-as-bench.md` → **Agent interface/spec** (the agent consumes benchmark instructions as a skill)
- Environment as code → **Executable benchmark world** (current example: `maze_game.py`)
- Outcome verifier → **Objective evaluation** (current example: `score_maze_run.py`)

```text
+---------------------------+
|      skill-as-bench.md    |
|  (agent-facing benchmark) |
+------------+--------------+
             |
             v
+---------------------------+
|    Environment as Code    |
|   (maze_game.py runtime)  |
+------------+--------------+
             |
             v
+---------------------------+
|      Outcome Verifier     |
|   (score_maze_run.py)     |
+------------+--------------+
             |
             v
+---------------------------+
|   Comparable Bench Result |
| (success, steps, optimal, |
|  efficiency, rating)      |
+---------------------------+
```

## Blockers
- None.

## Next steps
- Continue formalizing this 3-layer structure so every new benchmark has: skill spec, environment implementation, and verifier.
- Mirror these scoring conventions across new agent-bench tasks so benchmarks share a consistent evaluation schema.
- Add a simple aggregated runner later to compare multiple runs/agents on the same maze metric format.
